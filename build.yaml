# |retrofit_generator
targets:
  $default:
    builders:
      json_serializable|freezed:
        generate_for:
          - "{{}}.dart"
    sources:
      - "lib/**"
      - "test/**"
      - "data_module/**"
      - "domain_module/**"
